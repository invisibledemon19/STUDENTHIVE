/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  allowedDevOrigins: ['localhost', '127.0.0.1', '0.0.0.0', 'studenthive.local', '*.studenthive.local'], // Add your specific dev origins here
};

export default nextConfig;
